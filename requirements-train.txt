# Phase 2 — Training Pipeline (Prop-DeOccNet)
# Install: pip install -r requirements-train.txt
#
# Pilih versi torch sesuai hardware:
#
# [A] CUDA GPU (NVIDIA):
#   pip install torch==2.2.2 torchvision==0.17.2 --index-url https://download.pytorch.org/whl/cu121
#
# [B] AMD/Intel GPU via DirectML (Windows, tanpa ROCm):
#   pip install torch==2.2.2 torchvision==0.17.2
#   pip install torch-directml
#
# [C] CPU only:
#   pip install torch==2.2.2 torchvision==0.17.2
#
# Lanjutkan dengan dependensi di bawah (sama untuk semua):
#
# PENTING — pycocotools harus dikompilasi dari source (bukan binary wheel):
#   pip install "cython>=3.0.0"
#   pip install pycocotools --no-binary pycocotools --no-cache-dir
#
# Lokal (Windows CPU/DirectML): pin numpy 1.26.4 karena tidak ada konflik dengan opencv/jax
#   numpy==1.26.4
# Colab / Linux dengan opencv+jax: biarkan numpy 2.x, build pycocotools dari source
#
# Pastikan tensorflow TIDAK terinstall:
#   pip uninstall tensorflow tensorflow-intel keras tf-keras -y

# numpy: pin 1.26.4 untuk lokal; di Colab gunakan versi default (2.x)
numpy==1.26.4
albumentations==1.4.3
# pycocotools: install via: pip install pycocotools --no-binary pycocotools --no-cache-dir
pycocotools==2.0.7
tensorboard>=2.13.0
# scipy: >=1.14.0 untuk numpy 2.x; 1.13.0 cukup untuk numpy 1.x
scipy>=1.13.0
pyyaml>=6.0
tqdm>=4.65.0
pandas>=2.0.3
